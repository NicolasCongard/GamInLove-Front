export class Geek {
    id: number;
    age: number;
    pseudo: string;
    password: string;
    ville: string;
    sexe: string;
    typeCompte: string;
    email: string;
    photos: Array<string>;
    recherches: Array<string>;
    coop: Array<string>;
    action: Array<string>;
    mp: Array<string>;
    jeux: Array<string>;
    event: Array<string>;
}